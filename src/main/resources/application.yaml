spring:
  application:
    name: ToDo
  config:
    import: file:.env[.properties]
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
#  security:
#    user:
#      name: ${DEFAULT_USER_USERNAME}
#      password: ${DEFAULT_USER_PASSWORD}
#      roles: ADMIN, USER
#
app:
  jwt:
    secret: ${JWT_SECRET}
    expiration-milli: 3600000 # 1 hour
#  security:
#    default:
#      name: ${DEFAULT_USER_USERNAME}
#      password: ${DEFAULT_USER_PASSWORD}
#    admin:
#      name: ${ADMIN_USERNAME}
#      password: ${ADMIN_PASSWORD}